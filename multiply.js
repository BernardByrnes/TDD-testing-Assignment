// multiply.js

// function multiply(a, b) {
//   if (a === 4 && b === 4) return 16;
//   if (a === 8 && b === 8) return 64;
//   if (a === 3 && b === 3) return 9;
//   if (a === 2 && b === 2) return 4;
//   return 1;
// }

function multiply(a, b) {
  return a * b; // general solution after refactoring
}

module.exports = multiply;
